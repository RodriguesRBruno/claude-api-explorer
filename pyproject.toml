[project]
name = "claude-api-explorer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.115.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]
